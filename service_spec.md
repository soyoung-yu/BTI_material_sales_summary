  ---
  서비스 개요                                                                                                                                       
   
  목적: 팀별 개발 소재 기반 매출 집계 리포트 자동 생성                                                                                              
                  
  ---
  핵심 기능

  1. 팀/소재 관리

  - 팀별 개발 소재 목록 관리 (추가/수정/삭제)
  - 사용자는 소속 팀의 소재만 관리

  2. 리포트 생성
  - 기간 선택 : 시작일 ~ 종료일
  - 집계 방식 선택 : 모든 항목은 중복 선택 가능
    - 총 매출
    - 월별, 분기별, 반기별 (중복 선택 가능)
    - 소재별, 제품 라인별 (중복 선택 가능)
- 기본 시트 : Raw 데이터 (항상 포함)
- 추가 시트 : 선택한 집계 방식별로 시트 추가


  3. 엑셀 다운로드
  - 선택 옵션에 따라 xlsx 파일 생성 후 다운로드
  엑셀 스타일링 적용 내용:
  ┌───────────┬────────────────────────────────────────────────────────┐                                                                            
  │   항목    │                       적용 내용                        │
  ├───────────┼────────────────────────────────────────────────────────┤                                                                            
  │ 헤더      │ 진한 남색 배경 (#1A365D), 흰색 글씨, 볼드, 가운데 정렬 │                                                                          
  ├───────────┼────────────────────────────────────────────────────────┤                                                                            
  │ 테두리    │ 모든 셀에 얇은 테두리                                  │
  ├───────────┼────────────────────────────────────────────────────────┤
  │ 줄무늬    │ 짝수 행 연한 회색 배경                                 │
  ├───────────┼────────────────────────────────────────────────────────┤
  │ 숫자 포맷 │ 매출: 천단위 구분 (#,##0) / 비율: 퍼센트 (0.0%)        │
  ├───────────┼────────────────────────────────────────────────────────┤
  │ 정렬      │ 숫자는 우측 정렬, 텍스트는 좌측 정렬                   │
  ├───────────┼────────────────────────────────────────────────────────┤
  │ 컬럼 너비 │ 내용에 맞게 자동 설정                                  │
  ├───────────┼────────────────────────────────────────────────────────┤
  │ 필터      │ 헤더 행에 자동 필터 적용                               │
  ├───────────┼────────────────────────────────────────────────────────┤
  │ 고정      │ 첫 행(헤더) 고정                                       │
  └───────────┴────────────────────────────────────────────────────────┘

  엑셀 집계 중복 처리 기준(필수):
  - 기본 원칙: 제품 매출 중복 제거 키는 `mitem_code + customer_code + base_time`
  - `소재별(Summary_소재별)`을 제외한 모든 Summary 시트:
    - 위 키 기준으로 중복 제거 후 집계 (제품 매출 중복 합산 금지)
  - `소재별(Summary_소재별)`:
    - 소재 기여도 확인 목적이므로 중복 허용 집계 유지
    - 마지막 컬럼 `중복 금액(소재)` 추가
    - 표기 형식: `A&B:금액, A&C:금액` (예: `M-1001&M-1002:1,797,000`)
    - 의미: 해당 소재가 다른 소재와 같은 제품건에서 함께 사용되어 발생한 상대 소재별 중복 금액
  - `소재별(Summary_소재별)`의 `총매출 비중`:
    - 분모는 중복 제거 총매출이 아닌, 중복 허용 총매출 사용
    - 따라서 소재별 `총매출 비중` 합계는 100%가 되도록 계산


  ---
  추가 확인 사항

  1. 데이터 소스: Athena 쿼리를 서비스에서 직접 실행 (테스트 목업에서는 로컬 데이터 활용 sample_df.csv)
  2. 사용자 인증: 실제 서비스 운영 시에 사이트 접근 시 사용자 인증 정보가 붙을 것(user_name(사용자이름), dept_id(팀이름))
  3. 배포 환경: 사내서버 ec2
  4. 기술 스택 선호: Next.js + React + FastAPI

  향후 계획 메모:
  - 소재 데이터는 현재 목업용 하드코딩
  - 실제 서비스에서는 내부 DB (PostgreSQL 등)에서 관리
  - FastAPI 백엔드에서 CRUD API 제공 예정
